from setuptools import setup

setup(
    name="image_downloader",
    version="0.1",
    py_modules=["image_downloader"],
    install_requires=[
        "requests",
        "beautifulsoup4",
    ],
)
